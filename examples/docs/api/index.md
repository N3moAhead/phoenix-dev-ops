# API Overview

The example API page shows that nested `index.md` files are also resolved by the
router.

## Exported functions

- `startDocsServer(options)`
- `createDocsApp(options)`
- `summarizeDocument(markdown)`

## Expected output

When this page is rendered, the summary card should display a heading list, a
word count, and a short reading-time estimate.
