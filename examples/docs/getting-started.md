# Getting Started

Use this page when you want a lightweight manual demo of the repository.

## Local verification

Run the following commands:

```bash
npm ci
npm run format
npm run verify
```

## Manual browser check

Point the server to `./examples/docs` and open the configured base path in the
browser.
