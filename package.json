{
  "name": "phoenix-dev-ops",
  "version": "0.1.0",
  "description": "Lightweight Express-based markdown docs server",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "clean": "rm -rf dist",
    "deploy:prepare": "node scripts/prepare-deployment.mjs",
    "deploy:test": "npm run build && npm run deploy:prepare",
    "lint": "eslint src tests --ext .ts",
    "test:unit": "vitest run tests/index.test.ts tests/document-summary.test.ts",
    "format": "prettier --write .",
    "test:integration": "vitest run tests/integration.test.ts",
    "verify": "npm run lint && npm run build && npm run test:unit && npm run test:integration"
  },
  "keywords": [
    "docs",
    "express",
    "markdown",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/express": "^5.0.3",
    "@types/node": "^22.14.0",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^10.0.3",
    "typescript-eslint": "^8.56.1",
    "prettier": "^3.8.1",
    "typescript": "^5.8.2",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2",
    "vitest": "^4.0.18"
  }
}
